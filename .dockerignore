.venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Local artifacts and gathered data — not for the image.
benchmark_results.jsonl
final_crop.png
*.log

# Secrets — use docker compose env_file or `docker run -e` instead.
.env

# Docs, scripts, READMEs — only the runtime needs to be in the image.
docs/
scripts/
README*.md

# VCS, editor, OS junk.
.git/
.gitignore
.idea/
.vscode/
.DS_Store
